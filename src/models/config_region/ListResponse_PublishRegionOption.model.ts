import { PublishRegionOption } from '../common/PublishRegionOption.model';

export interface ListResponse_PublishRegionOption {
  /**
   * data
   */
  data: PublishRegionOption[] | null;
  /**
   * count
   */
  count: number | null;
}