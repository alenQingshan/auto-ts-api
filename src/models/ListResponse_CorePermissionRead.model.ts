import { CorePermissionRead } from './CorePermissionRead.model';

export interface ListResponse_CorePermissionRead {
  /**
   * data
   */
  data: CorePermissionRead[] | null;
  /**
   * count
   */
  count: number | null;
}