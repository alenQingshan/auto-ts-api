import { TTSVoiceRead } from './TTSVoiceRead.model';

export interface ListResponse_TTSVoiceRead {
  /**
   * data
   */
  data: TTSVoiceRead[] | null;
  /**
   * count
   */
  count: number | null;
}